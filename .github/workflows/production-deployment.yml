name: Production Deployment

on:
  workflow_dispatch: 
  push:
    branches:
      - main 

jobs:
  deploy-to-production:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: http://${{ secrets.PROD_IP }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.PROD_IP }}
          username: ${{ secrets.PROD_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            # Pull the latest Docker image
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/calculator-app:latest
            
            # Stop and remove existing container if running
            docker stop calculator-app || true
            docker rm calculator-app || true
            
            # Run the new container on port 80
            docker run -d \
              --name calculator-app \
              -p 80:3000 \
              --restart unless-stopped \
              ${{ secrets.DOCKERHUB_USERNAME }}/calculator-app:latest
            
            # Clean up unused images
            docker image prune -f

      - name: Verify Deployment
        run: |
          echo "Application deployed successfully!"
          echo "Access your app at: http://${{ secrets.PROD_IP }}"
